import dotenv from 'dotenv'; dotenv.config(); import('./server/aiProvider.js').then(async m => { console.log('Live AI:', m.isLiveAiConfigured()); const r = await m.generateLegalRoadmap({workflow:'rental',state:'Karnataka',description:'Landlord not returning security deposit',hasUrgentRisk:false},[],null); console.log('SUCCESS urgency:', r.urgency_level, 'steps:', r.immediate_steps?.length); }).catch(e => console.error('FAIL:', e.message));  
